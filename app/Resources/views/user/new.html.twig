{% extends 'base.html.twig' %}

{% block header %}
    <h1>{{ 'user_new_title'|trans({}, 'user') }}</h1>
{% endblock %}

{% block body %}
    {{ form_start(form) }}
        {{ form_widget(form) }}

            <input type="submit" class="btn btn-primary float-left mr-1" value="{{ 'new_button'|trans({}, 'general') }}" />
    {{ form_end(form) }}

    <a class="btn btn-default float-left mr-1" href="{{ path('admin_user_index') }}">{{ 'return_button'|trans({}, 'general') }}</a>
{% endblock %}
